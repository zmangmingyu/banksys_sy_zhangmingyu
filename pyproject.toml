[tool.coverage.run]
# Streamlit 页面是 UI 代码,由集成/E2E 测试覆盖,不计入单元测试覆盖率
omit = ["src/pages/*"]

[tool.ruff]

[tool.ruff.lint]
# Streamlit 多页应用文件名惯例含 emoji(to show sidebar icons),忽略命名规则
per-file-ignores = { "src/pages/*.py" = ["N999"] }

[tool.ruff.format]
line-ending = "auto"
